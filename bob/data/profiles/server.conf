[profile]
name        = server
description = Server profile — all checks at full severity (default)

# No overrides: every check uses its default severity and scoring.
# This profile is the baseline that all other profiles extend.
